[project]
name = "autobrewpi-workspace"
version = "0.0.0"
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["packages/worker", "packages/master"]

[dependency-groups]
dev = ["pytest>=8.0", "grpcio-tools>=1.62", "httpx>=0.27"]
